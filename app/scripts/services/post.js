'use strict';

/**
 * @ngdoc service
 * @name fakeApiApp.Post
 * @description
 * # Post
 * Service in the fakeApiApp.
 */
angular.module('fakeApiApp')
  .service('Post', function Post() {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
